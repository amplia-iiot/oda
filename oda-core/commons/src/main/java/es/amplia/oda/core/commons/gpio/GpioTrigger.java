package es.amplia.oda.core.commons.gpio;

/**
 * GPIO pin trigger mode representation.
 */
public enum GpioTrigger {
    NONE, FALLING_EDGE, RISING_EDGE, BOTH_EDGES, LOW_LEVEL, HIGH_LEVEL, BOTH_LEVELS
}
