package es.amplia.oda.datastreams.deviceinfo;

/**
 * Utility class with the default datastream identifiers.
 */
class DatastreamIds {
    static final String DEVICE_ID = "deviceId";
    static final String SERIAL_NUMBER = "provision.device.serialNumber";

    /**
     * Private constructor to avoid the instantiation of the class.
     */
    private DatastreamIds() {}
}
